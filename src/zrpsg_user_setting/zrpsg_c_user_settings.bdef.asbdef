projection;
strict ( 2 );
use draft;

define behavior for ZRPSG_C_USER_SETTINGS //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action Resume;
  use action Edit;
  use action Prepare;
  use action Activate;
  use action Discard;
}