projection;
strict ( 1 );
use draft;

define behavior for ZRPSG_C_AM_PTEXT alias PTEXT
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}